AMOUS_ADMIN_PASSWORD=AMOUS:)
AMOUS_SECRET_KEY=replace-with-a-long-random-secret
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-keep-private
SUPABASE_CLIENT_BUCKET=client-logos
# AI agent settings - do NOT commit your real key. Create `backend/.env` with these values.
# AGENT_API_KEY=sk-...    # AI provider secret key (keep private, do NOT check into git)
# AGENT_API_URL=https://api.openai.com/v1/chat/completions  # Optional, defaults to OpenAI Chat Completions
